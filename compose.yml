services:
  auth-service:
    image: tobiasbrandy/auth-service:latest
    restart: always
    expose:
      - "80"
    environment:
      APP__ENV: ${APP__ENV:-dev}
      APP__AUTH__JWT_SECRETS: ${APP__AUTH__JWT_SECRETS:-'{}'}

  app-service:
    image: tobiasbrandy/app-service:latest
    restart: always
    expose:
      - "80"
    depends_on:
      - auth-service

  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service
      - app-service
services:
  auth-service:
    build:
      context: .
      target: auth-service
    ports:
      - "3000:80"

  app-service:
    build:
      context: .
      target: app-service
    ports:
      - "8000:80"

  nginx:
    ports:
      - "8080:80"
